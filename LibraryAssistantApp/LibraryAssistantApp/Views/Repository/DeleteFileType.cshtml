@model LibraryAssistantApp.Models.Document_Type

@{
    ViewBag.Title = "Delete File Type";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>DeleteFileType</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <div class="row">
        <div class="col-md-6">
            <div class="panel panel-default">
                <table class="table">
                    <tr>
                        <td><b>@Html.DisplayNameFor(model => model.Document_Type_Name)</b></td>
                        <td style="padding-left:5px">@Html.DisplayFor(model => model.Document_Type_Name)</td>
                    </tr>
                    <tr>
                        <td><b>@Html.DisplayNameFor(d => d.Description)</b></td>
                        <td style="padding-left:5px">@Html.DisplayFor(d => d.Description)</td>
                    </tr>
                </table>
            </div>
        </div>
    </div>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-primary" /> |
            @Html.ActionLink("Back to List", "ViewFileType")
        </div>
    }
</div>
