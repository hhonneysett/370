@model IEnumerable<LibraryAssistantApp.Models.venueRating>

<div id="availableVenues" class="col-md-12">
    @{
        if (Model.Any())
        {
            <div id="venueDetails"><span id="venueArrow" class="glyphicon glyphicon-triangle-bottom"></span><b>Select A Venue:</b></div>

            <hr />

            <div id="toggleVenue">
                <div class="panel panel-default">
                    <table class="table" id="venueTable">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.venue.Venue_Name)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.venue.Capacity)
                                </th>                                
                                <th>
                                    @Html.DisplayNameFor(model => model.venue.Venue_Type1.Description)
                                </th>
                                <th>
                                    Characteristics
                                </th>
                                <th>
                                    Match
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr onclick="AssignButtonClicked(this)" data-assigned-id="@item.venue.Venue_ID">
                                    <td>
                                        @Html.DisplayFor(modelItem => item.venue.Venue_Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.venue.Capacity)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.venue.Venue_Type1.Description)
                                    </td>
                                    <td>
                                        @item.characteristics
                                    </td>
                                    <td>
                                        @item.rating %
                                    </td>
                                </tr>
                            }
                        </tbody>                                               
                    </table>
                </div>

                    <input type="button" id="btnProceed" class="btn btn-default" value="Proceed"/>
            </div>
            
            
        }
        else
        {
            <p>No Venues Available</p>
        }
    }
</div>
